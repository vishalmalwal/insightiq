# Copy to .env.local for local dev (optional) or set in Vercel project settings.
# Leave unset in dev — the Vite proxy forwards /api to http://localhost:8000.
# In production, point at the deployed API origin so the browser calls it directly:
# VITE_API_BASE=https://insightiq-api.onrender.com
VITE_API_BASE=
